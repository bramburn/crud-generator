    /**
     * Show the form for creating a new resource.
     *
     * @return \Illuminate\View\View
     */
    public function create(ParentModel $parent)
    {
    	$parentlist = $parent->all();

        return view('{{viewPath}}{{viewContainerFolder}}.create', compact('parentlist'));
    }